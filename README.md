# Webhook Trigger

```bash
usage: app.py [-h] [--debug] [--port PORT] path

positional arguments:
  path         monitoring git path, git will pull if triggered

optional arguments:
  -h, --help   show this help message and exit
  --debug      show debug information
  --port PORT  listening port
```
